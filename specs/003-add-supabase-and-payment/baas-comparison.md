# BaaS 平台对比：Auth + Database + Payment 解决方案

**目标**：寻找月费 < $10 的 Supabase 替代方案
**场景**：Podscript 转写服务的用户认证、积分系统、支付集成
**评估日期**：2026-01-04

---

## 候选平台概览

| 平台 | 类型 | 数据库 | 开源 | 自托管 |
|------|------|--------|------|--------|
| [Supabase](https://supabase.com) | 托管/自托管 | PostgreSQL | ✅ | ✅ |
| [PocketBase](https://pocketbase.io) | 自托管 | SQLite | ✅ | ✅ |
| [Appwrite](https://appwrite.io) | 托管/自托管 | MariaDB | ✅ | ✅ |
| [Nhost](https://nhost.io) | 托管/自托管 | PostgreSQL + Hasura | ✅ | ✅ |
| [Firebase](https://firebase.google.com) | 托管 | Firestore (NoSQL) | ❌ | ❌ |

---

## 详细对比

### 1. Supabase

**简介**：开源 Firebase 替代品，基于 PostgreSQL，提供实时订阅、Row Level Security。

| 维度 | 评分 | 说明 |
|------|------|------|
| **月费** | ⭐⭐⭐ | 免费层：500MB DB, 50K MAU；Pro: $25/月；自托管: ~$5-10/月 |
| **代码复杂度** | ⭐⭐⭐⭐ | SDK 完善，Python/JS 支持好，文档详尽 |
| **机器需求** | ⭐⭐ | 自托管需 Docker，建议 2GB+ RAM |
| **Auth 功能** | ⭐⭐⭐⭐⭐ | Email/OAuth/Magic Link，JWT，完整用户管理 |
| **支付集成** | ⭐⭐⭐ | 需自己集成 Stripe/其他，有社区示例 |
| **扩展性** | ⭐⭐⭐⭐⭐ | PostgreSQL，生产级扩展能力 |

**优点**：
- 成熟的 PostgreSQL 生态
- RLS 实现行级安全
- 实时订阅功能
- 官方 Python SDK

**缺点**：
- 自托管配置复杂（多个服务）
- 免费层有限制（7天不活跃暂停）

**自托管成本**：
```
VPS: $5-10/月 (2GB RAM, Hetzner/Vultr)
总计: ~$5-10/月
```

---

### 2. PocketBase

**简介**：单文件 Go 后端，内置 SQLite、Auth、实时订阅。极简主义者的选择。

| 维度 | 评分 | 说明 |
|------|------|------|
| **月费** | ⭐⭐⭐⭐⭐ | 完全免费，仅需 VPS 费用 $4-6/月 |
| **代码复杂度** | ⭐⭐⭐⭐⭐ | 单文件部署，API 简洁，学习曲线低 |
| **机器需求** | ⭐⭐⭐⭐⭐ | 极低：1 vCPU, 1GB RAM 即可运行 |
| **Auth 功能** | ⭐⭐⭐⭐ | Email/OAuth，自定义字段，但无 RBAC |
| **支付集成** | ⭐⭐ | 需完全自己实现，无官方指南 |
| **扩展性** | ⭐⭐ | SQLite 限制，不适合高并发写入 |

**优点**：
- 单个二进制文件，部署极简
- 资源占用极低（10K+ 并发仅需 4GB RAM）
- 内置管理面板
- 实时订阅支持

**缺点**：
- SQLite 不适合大规模应用
- 无内置 RBAC
- 社区较小，第三方集成少
- 无官方 Python SDK（需用 REST API）

**自托管成本**：
```
VPS: $4-6/月 (1GB RAM, Hetzner CAX11)
总计: ~$4-6/月
```

---

### 3. Appwrite

**简介**：模块化开源 BaaS，支持多语言 SDK，功能全面。

| 维度 | 评分 | 说明 |
|------|------|------|
| **月费** | ⭐⭐⭐⭐ | 云版免费层有限；自托管免费 |
| **代码复杂度** | ⭐⭐⭐⭐ | 多语言 SDK，文档好，但概念较多 |
| **机器需求** | ⭐⭐ | Docker 部署，建议 4GB+ RAM |
| **Auth 功能** | ⭐⭐⭐⭐⭐ | 完整认证，30+ OAuth 提供商，MFA |
| **支付集成** | ⭐⭐⭐ | 需自己集成，有 Stripe 示例 |
| **扩展性** | ⭐⭐⭐⭐ | MariaDB，支持水平扩展 |

**优点**：
- 官方 SDK：Python, JS, Flutter, Swift, Kotlin
- 内置 Functions (支持 Python)
- 完整的权限系统
- 活跃社区

**缺点**：
- 自托管资源消耗较大
- Docker 多容器部署
- 云版免费层限制多

**自托管成本**：
```
VPS: $10-15/月 (4GB RAM)
总计: ~$10-15/月
```

---

### 4. Nhost

**简介**：基于 Hasura + PostgreSQL 的 GraphQL-first 后端。

| 维度 | 评分 | 说明 |
|------|------|------|
| **月费** | ⭐⭐⭐⭐ | 免费层：1GB DB, 5GB 带宽；Pro: $25/月 |
| **代码复杂度** | ⭐⭐⭐ | GraphQL 学习曲线，但自动生成 API |
| **机器需求** | ⭐⭐ | Hasura + PostgreSQL，需 4GB+ RAM |
| **Auth 功能** | ⭐⭐⭐⭐ | 完整认证，社交登录，自定义声明 |
| **支付集成** | ⭐⭐⭐ | 需自己集成 |
| **扩展性** | ⭐⭐⭐⭐ | PostgreSQL + Hasura，生产级 |

**优点**：
- GraphQL 自动生成
- Hasura 实时订阅
- 强大的权限系统
- 可自托管

**缺点**：
- 免费层限制（1周不活跃暂停，仅1个活跃项目）
- GraphQL 学习曲线
- 自托管复杂

**自托管成本**：
```
VPS: $10-15/月 (4GB RAM)
总计: ~$10-15/月
```

---

### 5. Firebase

**简介**：Google 的 BaaS，成熟稳定，但闭源且可能成本失控。

| 维度 | 评分 | 说明 |
|------|------|------|
| **月费** | ⭐⭐ | 免费层慷慨但易超；按量付费可能昂贵 |
| **代码复杂度** | ⭐⭐⭐⭐ | SDK 成熟，文档完善 |
| **机器需求** | ⭐⭐⭐⭐⭐ | 无需自托管 |
| **Auth 功能** | ⭐⭐⭐⭐⭐ | 行业标杆，完整认证 |
| **支付集成** | ⭐⭐⭐⭐ | Firebase Extensions 有 Stripe 集成 |
| **扩展性** | ⭐⭐⭐⭐⭐ | Google 基础设施 |

**优点**：
- 成熟稳定
- 优秀的移动端支持
- 丰富的 Extensions
- 无需运维

**缺点**：
- 厂商锁定
- NoSQL 不适合复杂查询
- 成本不可预测
- 不开源，无法自托管

**月费估算**（按使用量）：
```
低流量: 免费
中流量: $10-50/月
高流量: $100+/月
```

---

## 综合评分表

| 平台 | 月费 | 代码复杂度 | 机器需求 | Auth | 支付集成 | 扩展性 | **总分** |
|------|------|-----------|---------|------|---------|--------|---------|
| **PocketBase** | 5 | 5 | 5 | 4 | 2 | 2 | **23** |
| **Supabase** | 3 | 4 | 2 | 5 | 3 | 5 | **22** |
| **Appwrite** | 4 | 4 | 2 | 5 | 3 | 4 | **22** |
| **Nhost** | 4 | 3 | 2 | 4 | 3 | 4 | **20** |
| **Firebase** | 2 | 4 | 5 | 5 | 4 | 5 | **25** |

*评分说明：5分最优，1分最差*

---

## 按场景推荐

### 场景 A：最低成本（< $5/月）

**推荐：PocketBase**

```bash
# 部署到 $4/月 Hetzner VPS
wget https://github.com/pocketbase/pocketbase/releases/download/v0.x/pocketbase_x_linux_amd64.zip
unzip pocketbase_*.zip
./pocketbase serve --http=0.0.0.0:8090
```

- 月费：~$4-6
- 适合：原型、个人项目、小型 SaaS
- 限制：SQLite 性能上限

### 场景 B：生产级 + 预算敏感（< $10/月）

**推荐：Supabase 自托管**

```bash
# 使用 Docker Compose 部署
git clone https://github.com/supabase/supabase
cd supabase/docker
cp .env.example .env
docker compose up -d
```

- 月费：~$6-10（2GB VPS）
- 适合：需要 PostgreSQL、RLS、实时功能的项目
- 优势：可随时迁移到 Supabase Cloud

### 场景 C：零运维（使用云服务免费层）

**推荐：Supabase Cloud 免费层**

- 500MB 数据库
- 1GB 存储
- 50,000 MAU
- 足够小型项目使用

### 场景 D：需要 GraphQL

**推荐：Nhost 免费层 或 自托管**

- 自动 GraphQL API 生成
- 强大的实时订阅
- 适合前端密集型应用

---

## Podscript 项目推荐

基于 Podscript 的需求（Auth + 积分 + 支付 + 转写记录）：

### 🏆 首选：Supabase（当前方案）

**理由**：
1. PostgreSQL 适合积分事务处理
2. RLS 实现用户数据隔离
3. Python SDK 与现有 FastAPI 集成好
4. 可从免费层开始，按需升级

**成本路径**：
```
开发阶段: 免费层
上线初期: 免费层 (< 50K MAU)
规模增长: Pro $25/月 或 自托管 $10/月
```

### 🥈 备选：PocketBase

**适用场景**：如果想要极简部署和最低成本

**优势**：
- 单文件部署到任意 VPS
- $4/月 即可运行
- 足够支撑 10K+ 用户

**劣势**：
- SQLite 事务性能不如 PostgreSQL
- 需要自己写更多业务逻辑
- 无官方 Python SDK

### ⚠️ 不推荐：Firebase

**理由**：
- 厂商锁定严重
- 中国区访问问题
- NoSQL 不适合积分账本类应用
- 成本不可控

---

## 支付集成对比

所有 BaaS 平台都不直接提供支付功能，需要集成第三方：

| 平台 | 国际支付 | 国内支付 | 集成难度 |
|------|---------|---------|---------|
| Supabase | Stripe（有示例） | 需自己集成 Z-Pay 等 | ⭐⭐⭐ |
| PocketBase | 需完全自己实现 | 需完全自己实现 | ⭐⭐ |
| Appwrite | Stripe（有示例） | 需自己集成 | ⭐⭐⭐ |
| Firebase | Stripe Extension | 需自己集成 | ⭐⭐⭐⭐ |

**结论**：对于国内支付（Z-Pay、微信、支付宝），所有平台都需要自己实现集成逻辑，复杂度相近。

---

## 迁移难度评估

如果未来需要切换平台：

| 从 → 到 | 难度 | 说明 |
|---------|------|------|
| Supabase → PocketBase | ⭐⭐⭐ | 需重写 RLS 逻辑，数据迁移中等 |
| PocketBase → Supabase | ⭐⭐ | SQLite 导出，PostgreSQL 导入 |
| Supabase → 自建 PostgreSQL | ⭐ | 标准 pg_dump 即可 |
| Firebase → 任意 | ⭐⭐⭐⭐⭐ | 困难，数据模型差异大 |

---

## 最终建议

### 对于 Podscript：继续使用 Supabase ✅

1. **PostgreSQL** 事务支持适合积分扣减/充值
2. **RLS** 简化安全逻辑
3. **成熟生态** 减少开发时间
4. **灵活部署** 可从云服务迁移到自托管

### 如果预算极其有限：考虑 PocketBase

但需要接受：
- 更多手写代码
- SQLite 性能限制
- 较小的社区支持

---

## 参考资料

- [Supabase vs Firebase vs PocketBase 对比](https://www.supadex.app/blog/supabase-vs-firebase-vs-pocketbase-which-one-should-you-choose-in-2025)
- [Top 10 Supabase Alternatives](https://blog.tooljet.com/supabase-alternatives/)
- [PocketBase 官方 FAQ](https://pocketbase.io/faq/)
- [Appwrite vs Supabase 对比](https://appwrite.io/blog/post/appwrite-compared-to-supabase)
- [Nhost 定价](https://nhost.io/pricing)
- [PocketBase VPS 托管指南](https://hostadvice.com/vps/pocketbase-hosting/)
- [Self-hosted BaaS 列表](https://gist.github.com/PARC6502/ee4db400a05e6eb6d0981bb8cd4e4c1c)
