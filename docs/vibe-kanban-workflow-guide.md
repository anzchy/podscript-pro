# Vibe Kanban 开发工作流指南

## 概述

本文档详细说明如何使用 [Vibe Kanban](https://github.com/BloopAI/vibe-kanban) 从 `tasks.md` 初始化并管理 Podscript 项目的 002-Add-Transcribe-History 功能开发。

Vibe Kanban 是一个 AI 代码代理协调平台，支持 Claude Code、Gemini CLI、Codex 等多种 AI 代理，通过看板界面管理任务执行。

---

## 前置要求

- Node.js 18+ 已安装
- Git 已配置
- Anthropic API key（用于 Claude Code）
- 项目已克隆到本地

---

## 第一部分：环境准备

### 步骤 1：安装并认证 Claude Code

```bash
# 安装并运行 Claude Code CLI
npx -y @anthropic-ai/claude-code

# 按提示完成认证流程（需要 Anthropic API key）
# 认证成功后会显示 "Authentication successful"
```

**完成功能**：安装 Anthropic 官方 CLI 工具，建立与 Claude API 的认证连接。

### 步骤 2：启动 Vibe Kanban

```bash
# 启动 Vibe Kanban 本地服务
npx vibe-kanban

# 默认会在 http://localhost:5173 启动 Web 界面
# 首次运行会引导你完成初始设置
```

**完成功能**：启动任务协调平台，打开 Web 管理界面。

---

## 第二部分：项目初始化

### 步骤 3：在 Vibe Kanban 中创建项目

1. 打开浏览器访问 `http://localhost:5173`
2. 点击 **Create Project** 按钮
3. 选择 **From existing git repository**
4. 浏览并选择 Podscript 项目目录：
   ```
   /Users/jackcheng/Documents/01_Coding/Front-end-projects/podscript
   ```
5. 项目名称设置为：`podscript-history-feature`

**完成功能**：将 Podscript 仓库关联到 Vibe Kanban 项目。

### 步骤 4：配置项目设置

点击项目页面右上角的 **设置按钮**，配置以下内容：

#### 4.1 Setup Scripts（环境准备脚本）

```bash
# 进入项目目录
cd /Users/jackcheng/Documents/01_Coding/Front-end-projects/podscript

# 创建并激活虚拟环境
python3 -m venv .venv
source .venv/bin/activate

# 安装依赖
pip install -r requirements.txt
pip install filelock jieba
```

在 Vibe Kanban 设置中输入：
```
source .venv/bin/activate && pip install -r requirements.txt
```

#### 4.2 Dev Server Scripts（开发服务器脚本）

```
source .venv/bin/activate && PYTHONPATH=./src uvicorn podscript_api.main:app --port 8001 --reload
```

#### 4.3 Cleanup Scripts（清理脚本）

```
source .venv/bin/activate && black . && isort .
```

**完成功能**：配置 AI 代理执行任务前的环境准备、开发服务器启动和代码格式化。

---

## 第三部分：从 tasks.md 创建任务

由于 Vibe Kanban 不直接支持从 markdown 文件导入，需要手动或通过 MCP 批量创建任务。

### 方式一：手动创建任务（推荐初次使用）

#### 步骤 5：创建 Phase 1 任务

在 Vibe Kanban 界面，按 `c` 键或点击 **+** 按钮创建任务：

**Task 1: T001 - Setup Dependencies**
```
Title: T001 安装新依赖

Description:
安装 filelock 和 jieba 依赖，更新 requirements.txt

执行命令：
pip install filelock jieba
pip freeze > requirements.txt

验收标准：
- requirements.txt 包含 filelock 和 jieba
- pip list 显示已安装
```

**Task 2: T002 - Add Pydantic Models**
```
Title: T002 [P] 添加 Pydantic 模型

Description:
在 src/podscript_shared/models.py 中添加 HistoryRecord 和 HistoryIndex 模型

参考 data-model.md 中的字段定义：
- HistoryRecord: task_id, title, source_url, source_type, media_type, duration, file_size, tags, created_at, viewed, thumbnail_url, status
- HistoryIndex: version, updated_at, records

验收标准：
- 模型定义符合 data-model.md 规范
- 包含所有必要的字段验证
```

**Task 3: T003 - Create Keywords Module**
```
Title: T003 [P] 创建关键词提取模块

Description:
在 src/podscript_shared/keywords.py 中创建关键词提取功能

使用 jieba.analyse.extract_tags() 实现 TF-IDF 关键词提取

def extract_keywords(text: str, topK: int = 5) -> list[str]:
    pass

验收标准：
- 函数能从中文文本提取关键词
- 返回指定数量的关键词列表
```

#### 步骤 6：创建 Phase 2 任务（Foundational）

依次创建 T004-T012 任务，每个任务包含：
- 清晰的标题（带任务 ID）
- 详细的描述和实现要求
- 具体的文件路径
- 验收标准

**示例 - T004:**
```
Title: T004 创建 HistoryManager 类

Description:
在 src/podscript_shared/history.py 中创建 HistoryManager 类

要求：
1. 使用 filelock 实现文件锁
2. 初始化时传入 history.json 路径
3. 实现 load() 和 save() 基础方法

class HistoryManager:
    def __init__(self, history_path: Path):
        self.history_path = history_path
        self.lock = FileLock(f"{history_path}.lock")

验收标准：
- 类能正确初始化
- 文件锁机制正常工作
```

### 方式二：通过 Claude Desktop MCP 批量创建

如果已配置 Claude Desktop 的 MCP 集成：

#### 步骤 5b：使用 Claude Desktop 批量创建

1. 打开 Claude Desktop
2. 确保 Vibe Kanban MCP server 已连接
3. 发送以下提示：

```
请根据以下 tasks.md 内容，在 Vibe Kanban 项目 "podscript-history-feature" 中批量创建任务：

[粘贴 specs/002-add-transcribe-history/tasks.md 内容]

每个任务需要包含：
- 标题：任务 ID + 简短描述
- 描述：详细实现要求、文件路径、验收标准
- 标签：Phase 编号、User Story 编号（如有）
```

**完成功能**：将 tasks.md 中的 45 个任务导入到 Vibe Kanban 看板。

---

## 第四部分：执行任务

### 步骤 7：配置代理并启动任务

1. 在看板中点击第一个任务 (T001)
2. 配置执行参数：
   - **Agent profile**: `CLAUDE_CODE`
   - **Variant**: `DEFAULT`
   - **Base branch**: `002-add-transcribe-history`
3. 点击 **Start Attempt** 开始执行

```
任务状态流转：
To Do → In Progress → In Review → Done
```

**完成功能**：Claude Code 开始在隔离的 git worktree 中执行任务。

### 步骤 8：监控任务执行

```bash
# 在终端查看 Claude Code 输出
# Vibe Kanban 会实时显示执行日志

# 任务完成后，状态自动变为 "In Review"
```

界面操作：
- 查看执行日志：点击任务卡片查看详情
- 查看代码变更：点击 **View Diff** 按钮
- 启动开发服务器：点击 **Start Dev Server** 按钮验证

**完成功能**：实时监控 AI 代理的代码生成过程。

### 步骤 9：代码审查

任务进入 "In Review" 状态后：

1. 点击任务查看生成的代码变更
2. 点击 **Start Dev Server** 启动本地服务验证
3. 检查代码质量和功能正确性
4. 如果满意，点击 **Complete** 或 **Merge**
5. 如果需要修改，创建新的 **Attempt**

```bash
# 手动验证（在项目目录）
source .venv/bin/activate
PYTHONPATH=./src pytest tests/test_history.py -v
```

**完成功能**：审查 AI 生成的代码，确保符合要求。

### 步骤 10：合并代码

审查通过后：

1. 点击 **Merge** 按钮将变更合并到主分支
2. 任务自动移动到 **Done** 列
3. 继续下一个任务

```
Git 操作（自动执行）：
git checkout 002-add-transcribe-history
git merge --no-ff worktree-branch
```

**完成功能**：将审查通过的代码合并到功能分支。

---

## 第五部分：并行执行策略

### 步骤 11：并行执行标记为 [P] 的任务

tasks.md 中标记 `[P]` 的任务可以并行执行：

```
Phase 1 并行任务：
- T002 [P] models.py
- T003 [P] keywords.py

Phase 3 (US1) 并行任务：
- T013 [P] test GET /history
- T014 [P] test GET /history/{id}
- T017 [P] index.html
- T018 [P] history.js
```

操作方式：
1. 同时选中多个 [P] 任务
2. 为每个任务配置相同的代理和分支
3. 点击 **Start All** 或逐个点击 **Start Attempt**
4. Vibe Kanban 会在不同的 worktree 中并行执行

**完成功能**：充分利用并行能力，加速开发进度。

---

## 第六部分：完整开发流程

### 推荐的执行顺序

```
┌─────────────────────────────────────────────────────────┐
│ Phase 1: Setup (T001-T003)                              │
│   └── 并行: T002, T003                                  │
└───────────────────────┬─────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Phase 2: Foundational (T004-T012)                       │
│   └── 顺序执行，每个任务依赖前一个                        │
└───────────────────────┬─────────────────────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────┐
│ Phase 3: US1 历史列表 (T013-T023) 🎯 MVP                │
│   └── 并行: T013+T014, T017+T018                        │
│   └── 完成后验证: 首页显示历史记录，点击跳转              │
└───────────────────────┬─────────────────────────────────┘
                        ▼
            ┌───────────┴───────────┐
            ▼                       ▼
┌───────────────────┐   ┌───────────────────┐
│ Phase 4: US2      │   │ Phase 5: US3      │
│ 操作菜单          │   │ 完整历史页        │
│ (T024-T033)       │   │ (T034-T039)       │
└─────────┬─────────┘   └─────────┬─────────┘
          └───────────┬───────────┘
                      ▼
┌─────────────────────────────────────────────────────────┐
│ Phase 6: Polish (T040-T045)                             │
│   └── 并行: T040, T041, T043                            │
└─────────────────────────────────────────────────────────┘
```

### MVP 检查点

完成 Phase 1-3 后执行 MVP 验证：

```bash
# 1. 运行测试
source .venv/bin/activate
PYTHONPATH=./src pytest --cov=src --cov-report=term-missing

# 2. 启动服务
PYTHONPATH=./src uvicorn podscript_api.main:app --port 8001

# 3. 测试功能
# - 完成一次转写任务
# - 刷新首页查看历史记录
# - 点击记录跳转到结果页
```

---

## 第七部分：常用命令速查

### Vibe Kanban 快捷键

| 快捷键 | 功能 |
|--------|------|
| `c` | 创建新任务 |
| `Enter` | 打开选中任务 |
| `Esc` | 关闭对话框 |
| `?` | 显示帮助 |

### 项目命令

```bash
# 启动 Vibe Kanban
npx vibe-kanban

# 启动开发服务器（手动）
source .venv/bin/activate
PYTHONPATH=./src uvicorn podscript_api.main:app --port 8001 --reload

# 运行测试
PYTHONPATH=./src pytest --disable-warnings

# 代码格式化
black . && isort .

# 查看测试覆盖率
PYTHONPATH=./src pytest --cov=src --cov-report=html
```

### Git 操作

```bash
# 查看当前分支
git branch

# 切换到功能分支
git checkout 002-add-transcribe-history

# 查看变更
git diff

# 提交变更
git add . && git commit -m "feat: add history feature"
```

---

## 第八部分：故障排除

### 问题 1：Claude Code 认证失败

```bash
# 重新认证
npx -y @anthropic-ai/claude-code --reset-auth

# 检查 API key
echo $ANTHROPIC_API_KEY
```

### 问题 2：任务执行卡住

1. 点击任务查看日志
2. 检查是否有依赖缺失
3. 点击 **Stop Attempt** 终止
4. 修复问题后创建新的 Attempt

### 问题 3：代码冲突

```bash
# 在 worktree 中解决冲突
git status
git add .
git commit -m "resolve conflicts"
```

Vibe Kanban 提供冲突解决界面，按提示操作。

### 问题 4：开发服务器无法启动

```bash
# 检查端口占用
lsof -i :8001

# 杀死占用进程
kill -9 <PID>

# 检查虚拟环境
which python3
```

---

## 附录：tasks.md 任务清单

完整任务列表位于：`specs/002-add-transcribe-history/tasks.md`

| Phase | 任务数 | 描述 |
|-------|--------|------|
| Phase 1 | 3 | Setup & Dependencies |
| Phase 2 | 9 | HistoryManager Core |
| Phase 3 | 11 | US1 历史列表（MVP） |
| Phase 4 | 10 | US2 操作菜单 |
| Phase 5 | 6 | US3 完整历史页 |
| Phase 6 | 6 | Polish |
| **Total** | **45** | |

---

## 参考资源

- [Vibe Kanban 官方文档](https://vibekanban.com/docs)
- [Claude Code 文档](https://docs.anthropic.com/claude-code)
- [Podscript 项目 CLAUDE.md](../CLAUDE.md)
- [功能规范 spec.md](../specs/002-add-transcribe-history/spec.md)
- [实现计划 plan.md](../specs/002-add-transcribe-history/plan.md)
