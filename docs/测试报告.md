# 测试报告（MVP）

- 测试框架：pytest + pytest-cov
- 覆盖率阈值：80%（模块维度）
- 单元测试：管线核心步骤与 API 创建/查询
- 集成测试：演示版以内存/文件系统为主；外部服务以桩替代
- 性能指标：
  - `POST /tasks` 本地 P95 预计≤500ms（同步路径轻量）
  - 端到端耗时受后台任务影响，不计入同步 SLA

> 运行后将填充具体执行数据与覆盖率报表摘要。