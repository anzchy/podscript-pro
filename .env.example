# Podscript Configuration
# Copy this file to .env and fill in your values

# ============== Storage Provider ==============
# Choose your cloud storage provider: 'oss' (Alibaba) or 'cos' (Tencent)
STORAGE_PROVIDER=oss

# Storage bucket name
STORAGE_BUCKET=your-bucket-name

# Storage region
# For Alibaba OSS: cn-shanghai, cn-beijing, cn-hangzhou, etc.
# For Tencent COS: ap-shanghai, ap-beijing, ap-guangzhou, etc.
STORAGE_REGION=cn-shanghai

# Storage path prefix (folder within bucket)
# Files will be uploaded to: {bucket}/{prefix}/{filename}
# Default: tingwu-audio
# STORAGE_PREFIX=audio/podscript

# ============== Alibaba Cloud (for OSS and Tingwu) ==============
ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=

# Optional: STS temporary credentials
# ALIBABA_CLOUD_SECURITY_TOKEN=

# Tingwu ASR service
TINGWU_ENABLED=1
TINGWU_APP_KEY=your-tingwu-app-key

# ============== Tencent Cloud (for COS) ==============
# Required only if STORAGE_PROVIDER=cos
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=

# ============== Supabase (Authentication & Database) ==============
# Get these from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...your-anon-key
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# JWT secret for token validation
# Get from: Supabase Dashboard → Settings → API → JWT Settings
SUPABASE_JWT_SECRET=your-jwt-secret

# ============== Z-Pay (Payment Gateway) ==============
# Get these from your Z-Pay merchant dashboard
ZPAY_PID=your-merchant-id
ZPAY_KEY=your-secret-key

# Webhook URL for payment notifications (must be publicly accessible)
ZPAY_NOTIFY_URL=https://your-domain.com/api/payment/webhook

# Return URL after payment completion
ZPAY_RETURN_URL=https://your-domain.com/static/payment-success.html

# ============== Optional Settings ==============
# Output directory for transcription results
ARTIFACTS_DIR=artifacts

# Qwen API key (if using Qwen services)
# QWEN_API_KEY=

# yt-dlp cookies browser (default: chrome)
# YTDLP_COOKIES_BROWSER=safari
